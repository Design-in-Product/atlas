# Non-macOS fallback: set these as environment variables
# On macOS, use --setup-email instead (stores in Keychain)
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=you@gmail.com
SMTP_PASS=your-app-password-here
SMTP_FROM=you@gmail.com
